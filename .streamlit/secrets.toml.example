# Streamlit Secrets Configuration (Example)
#
# For local testing:
# 1. Copy this file to secrets.toml (same directory)
# 2. Update the password below
# 3. secrets.toml is git-ignored for security
#
# For Streamlit Cloud deployment:
# - Set these secrets in the Streamlit Cloud dashboard
# - App Settings → Secrets → Paste the content below

# Password for accessing the app
password = "your-password-here"
