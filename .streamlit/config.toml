# Streamlit Configuration for Production Deployment

[server]
# Max upload size in MB (for PDF uploads)
maxUploadSize = 10

# Required for cloud deployment
headless = true

[browser]
# Disable usage stats
gatherUsageStats = false
